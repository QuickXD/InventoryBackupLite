name: JoinBackup
version: '${project.version}'
main: it.quick.joinbackup.JoinBackup
api-version: '1.13'
commands:
  inventorybackupper:
    description: "Comando per gestire il backup dell'inventario"
    permission: inventorybackupper.use
permissions:
  inventorybackupper.use:
    description: Permesso per utilizzare il comando /inventorybackupper loadinv
    default: op

